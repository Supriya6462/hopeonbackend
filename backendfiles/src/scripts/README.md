# Admin Creation Script

## Create Admin User

To create an admin user, run:

```bash
npm run create-admin
```

This will prompt you to enter:
- Admin name
- Admin email
- Admin password

The script will create a user with the `admin` role and `isOrganizerApproved: true`.
